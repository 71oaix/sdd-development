---
title: 常见错误（Gotchas）
source: sdd-development
type: gotchas
last-updated: 2026-09-03
---

# 常见错误与踩坑记录

> 格式：默认假设 → 实际规则 → 正确做法。

## G001 拿到需求直接写代码，跳过 plan

- **默认假设：** 需求明确就可以直接实现。
- **实际规则：** SDD 要求先文档后代码，没有 plan 就没有实现依据。
- **正确做法：** 至少产出最小 plan 并经人确认；用户明确豁免的小改动除外。

## G002 plan 只写步骤不写决策

- **默认假设：** plan 列出实现步骤就够了。
- **实际规则：** plan 的关键价值是「决策即上下文」——做了什么选择、放弃什么、为什么。
- **正确做法：** plan 必须含「关键决策」；遇到 plan 未覆盖的情况时据此判断。

## G003 UI 改动不给预览

- **默认假设：** 给代码或截图就能验收 UI。
- **实际规则：** 人无法从代码判断 UI 效果，验收必须看实际效果。
- **正确做法：** 涉及 UI 的 plan 必须提供设计图或 HTML 交互预览。

## G004 完成任务不更新文档

- **默认假设：** 代码完成即任务完成。
- **实际规则：** 文档是交付物的一部分，文档与代码脱节会让下一次任务失去上下文。
- **正确做法：** 改完代码同步改文档 + INDEX.md。

## G005 依赖对话记忆而非文档

- **默认假设：** 对话历史能承载任务规格。
- **实际规则：** 对话会压缩或切换，规格必须沉淀在 docs 里。
- **正确做法：** 规格在 docs/issues + docs/plans 里，随时可恢复。

## G006 忘记 plan review 和实现 review

- **默认假设：** 一个视角检查过就够了。
- **实际规则：** 独立 review 站在不同视角，能大幅提高准确性和稳定性。
- **正确做法：** plan 和实现各做一次独立 review（fresh context subagent）。

## G007 对不确定的问题跳过前置自检

- **默认假设：** issue 只要写出来就行，写歪了后面再改。
- **实际规则：** 问题定义错了，返工会发生在最贵的 plan / 实现阶段。
- **正确做法：** 命中触发条件（说不清目标 / 做错代价大 / 自己纠结）时，先跑一次 [issue-precheck](references/issue-precheck.md)。

## G008 把可选前置自检变成每个任务的强制步骤

- **默认假设：** 好方法就应该每次都执行。
- **实际规则：** 强制化会让流程变重，论证产出变成四平八稳的模板，失去价值。
- **正确做法：** 只在命中触发条件时使用；纯执行小任务直接跳过。

## G009 把完整源文件直接当成用户汇报

- **默认假设：** 用户需要看到全部背景、步骤和 review 才能决策。
- **实际规则：** 决策者先需要一个短摘要，源文件负责可追溯和深挖。
- **正确做法：** issue 先给问题/影响/优先级/立项建议；plan 先给方法/取舍/保守效果/待确认决策，附源文件链接。

## G010 把实施步骤当成方案决策内容

- **默认假设：** 具体文件、函数和命令越详细，plan 越容易批准。
- **实际规则：** 用户首先判断方法是否成立，不需要先阅读实现路线图。
- **正确做法：** 摘要讲抽象方法和取舍；实施步骤保留在源文件，用户需要时再展开。

## G011 用乐观预期替代效果证据

- **默认假设：** 方案看起来合理，就可以承诺性能、召回或质量会提升。
- **实际规则：** 结构性结果、概率性改善和未知效果必须分开。
- **正确做法：** 将效果分为“确定会得到 / 可能得到 / 目前未知”，没有基线就不写百分比。

## G012 把原始 review 噪声交给用户筛选

- **默认假设：** 用户应该阅读所有 reviewer 的中间意见。
- **实际规则：** Agent 负责吸收 review，并把修正后的最终方案交给用户决策。
- **正确做法：** review 仍写入源文件供审计，但汇报只给修正后的结论和仍需用户决定的事项。

## 新增 Gotcha 规则

> 每次调用后，若发现新的 edge case，询问用户是否追加到此文件。
> 用户确认后：检查是否重复 → 分配下一编号（G009...）→ 按格式写入 → 更新 last-updated。
